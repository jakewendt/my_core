<% content_for :top_menu, blogs_menu -%>
<% content_for :top_menu, legend -%>

<% controlframe "Blogs" do -%>
	(<%= "#{@blogs.total_entries}/#{current_user.blogs_count}" -%>)
	<%#= filter_select_tag %>
	<%= filter_and_sort_select_tags('Blog') %>
<% end -%>

<%= render :partial => "shared/filters" -%>
<%= render :partial => 'table' -%>

<%= will_paginate @blogs %>

<%= add_search_form(blogs_path,params) %>
