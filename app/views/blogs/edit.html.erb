<%= error_messages_for :blog %>

<% form_for(@blog) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
<%= f.submit "Update" %>&nbsp;
<%= link_to "Cancel and show", @blog, :class => 'button' %>
</p>
<% end %>
