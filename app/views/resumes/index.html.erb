<% content_for :top_menu, resumes_menu -%>
<% content_for :top_menu, legend -%>

<% controlframe "Resume" do -%>
	(<%= "#{@resumes.total_entries}/#{current_user.resumes_count}" -%>)
	<%#= filter_select_tag %>
	<%= filter_and_sort_select_tags('Resume') %>
<% end -%>

<%= render :partial => "shared/filters" -%>
<%= render :partial => 'table' -%>

<%= will_paginate @resumes %>

<%= add_search_form(resumes_path,params) %>
