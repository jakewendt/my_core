<%= note.title %>

<%= note.body %>

<%= "Tags: #{note.tag_names}" %>

<%= "Created on #{note.created_at.to_s(:basic)}" %>
<%= "Last updated on #{note.updated_at.to_s(:basic)}" %>

<%= "----------------------------------------------------------------------" %>

