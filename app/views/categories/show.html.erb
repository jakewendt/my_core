<% content_for :top_menu, assets_menu %>
<% content_for :top_menu, category_menu(@category) %>

<%= controlframe h("Assets categorized as #{h(@category.name)}") %>

<%= render :partial => 'assets/table', :collection => @assets, :as => :asset, 
	:locals => { :assets_length => @assets.length } %>

<%= will_paginate @assets %>
