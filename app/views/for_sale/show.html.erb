<% content_for :top_menu, asset_menu(@asset) if editable?(@asset) %>

<%= controlframe h(@asset.title), 'dollar_currency_sign.png' %>

<%= render :partial => 'show', :locals => { :asset => @asset } %>
