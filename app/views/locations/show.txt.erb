<%= 
	"\nAssets in location #{@location.name} by #{current_user.login}\n\n" <<
	"\nDownloaded on #{Time.now.to_s(:basic)}\n\n" <<
	"\n\n----------------------------------------------------------------------\n\n"
%>

<%= render :partial => 'assets/show',
	:collection => @location.assets,
	:as => :asset
-%>
