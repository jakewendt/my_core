<% content_for :top_menu, lists_menu -%>
<% content_for :top_menu, legend -%>

<% controlframe "Lists", 'note_accept.png' do -%>
	(<%= "#{@lists.total_entries}/#{current_user.lists_count}" -%>)
	<%#= filter_select_tag %>
	<%= filter_and_sort_select_tags('List') %>
<% end -%>

<%= render :partial => "shared/filters" -%>
<%= render :partial => 'table' -%>

<%= will_paginate @lists %>

<%= add_search_form(lists_path,params) %>
